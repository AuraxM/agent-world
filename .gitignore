node_modules/
*.log
.env
.env.local
.DS_Store
# Old SQLite databases that may have been left at root pre-refactor:
agent-world.db
agent-world.db-shm
agent-world.db-wal
# Build output (each subproject also has its own .gitignore):
dist/
.next/
.vite/
coverage/
tsconfig.tsbuildinfo
# Stray data/ logs left by tsx-run scripts:
data/
# Claude Code local settings (per-machine, not shared)
.claude/settings.local.json
